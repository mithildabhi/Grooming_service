import 'package:flutter/material.dart';

const Color kAccent = Color(0xFF2EE6D6);
const Color kTextDark = Color(0xFF0F172A);
const Color kHint = Color(0xFF94A3B8);
  
class AppColors {
  // 🌑 Core Backgrounds
  static const Color background = Color(0xFF0E1B1E); // Stitch dark teal
  static const Color surface = Color(0xFF13262A);
  static const Color glass = Color(0xCC162E33);

  // 🎯 Primary & Accent
  static const Color primary = Color(0xFF3EF2C4); // Aqua green
  static const Color primarySoft = Color(0xFF1E4E4A);
  static const Color accent = Color(0xFF5FFFE0);

  // 📝 Text
  static const Color textPrimary = Color(0xFFE9FEF7);
  static const Color textSecondary = Color(0xFF9ECBC1);
  static const Color textMuted = Color(0xFF6F9A92);
  static const Color textDisabled = Color(0xFF4F6F6A);

  // ⚠️ Status
  static const Color success = Color(0xFF4ADE80);
  static const Color warning = Color(0xFFFACC15);
  static const Color error = Color(0xFFF87171);

  // 🧊 Border / Divider
  static const Color divider = Color(0xFF1F3C40);
}
