cd backend

python --version

python -m venv venv

venv\Scripts\activate

pip install -r requirements.txt

python manage.py migrate

python manage.py createsuperuser

STEPS WHEN TERMINAL ASK FOR DATA        admin
                                        admin@gmail.com
                                        admin
                                        admin
                                        y
                                        y


python manage.py runserver




git checkout -b flutter-mithil

git add .

git checkout develop
git pull origin develop
git checkout flutter-mithil
git merge develop


git push origin flutter-mithil

git commit -m "Resolve conflict"


🔹 Step 1: Stash your local changes (SAVE THEM)

git stash push -m "my local work before sync"
git status

🔹 Step 2: Pull your friend’s changes

git pull origin main

🔹 Step 3: Create your own branch (IMPORTANT)

git checkout -b flutter-mithil

🔹 Step 4: Bring back YOUR code

git stash pop


🧯 If conflict appears (normal & fixable)
You’ll see markers like:
<<<<< HEAD
your code
=======
friend code
>>>>> main

git add .
git commit -m "Merge my changes with latest main"

🔹 Step 5: Push YOUR branch

git push origin flutter-mithil



Django REST Framework
Flutter & GetX
OpenStreetMap/Nominatim
Geolocator package

  geolocator: ^11.0.0
  permission_handler: ^11.2.0
  geocoding: ^2.1.1  # Optional, we're using Nominatim directly
  url_launcher: ^6.2.2

  add this TO YAML FILE